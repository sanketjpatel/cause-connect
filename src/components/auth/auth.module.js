(function (angular) {
    'use strict';

    /**
     * @namespace app.auth
     */
    angular
        .module('app.auth', [
            // 3rd Party Dependencies
            'ui.router'
        ]);
}(angular));
